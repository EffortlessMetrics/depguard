[package]
name = "depguard-app"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
description = "Use case orchestration for depguard (check, md, annotations, explain)"

[dependencies]
depguard-types = { version = "0.1.0", path = "../depguard-types" }
depguard-domain = { version = "0.1.0", path = "../depguard-domain" }
depguard-repo = { version = "0.1.0", path = "../depguard-repo" }
depguard-settings = { version = "0.1.0", path = "../depguard-settings" }
depguard-render = { version = "0.1.0", path = "../depguard-render" }

anyhow.workspace = true
camino.workspace = true
serde_json.workspace = true
time.workspace = true

[dev-dependencies]
tempfile.workspace = true
